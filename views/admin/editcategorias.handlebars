{{#each erros}}

<div class="alert alert-danger">{{texto}}</div>
    {{else}}

{{/each}}

<h3>Edit categoria</h3>
<div class="card">
<div class="card-body">
    <form action="/admin/categorias/edit" method="post">
    <input type="hidden" name="id" value="{{categorias._id}}">
    <label for="nome">Nome:</label>
        <input class = 'form-control'type="text" name="nome" id="nome" placeholder="Nome da categorias" value="{{categorias.nome}}">
    <label for="slug">Slug:</label>
        <input class = 'form-control' type="text" name="slug" id="slug" placeholder="Slug da categoria" value="{{categorias.slug}}">

<button type="submit" class="btn mt-2 btn-success" >Editar categoria</button>
</form>

</div>
</div>